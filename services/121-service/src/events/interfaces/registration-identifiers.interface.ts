export interface RegistrationIdentifiers {
  id: number; // This is used in the UI to link to the registration
  registrationProgramId: number; // This auto-incrementing field is shown in the activity overview in the UI
}
