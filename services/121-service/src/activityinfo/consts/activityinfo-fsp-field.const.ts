/**
 * Code of the special ActivityInfo field that selects which FSP configuration
 * an imported registration should use. It is named 'fsp' rather than
 * 'programFspConfigurationName' because that is less confusing for the
 * non-technical users who build the forms.
 */
export const fspFieldCode = 'fsp';
