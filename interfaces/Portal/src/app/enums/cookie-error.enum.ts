import { CookieErrors } from '../../../../../services/121-service/src/shared/enum/cookie.enums';
/**
 * Note: CookieErrors-definition explicitly used from back-end, to prevent repetition.
 */
const CookieError = CookieErrors;
type CookieError = CookieErrors;
export default CookieError;
